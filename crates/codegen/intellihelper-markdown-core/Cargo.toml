[package]
license = "Apache-2.0"
name = "intellihelper-markdown-core"
version = "0.1.0"
edition.workspace = true
description = "Headless markdown analysis sharing IntelliHelper CLI's exact pulldown-cmark config."

[dependencies]
pulldown-cmark = { workspace = true }

[lints]
workspace = true
